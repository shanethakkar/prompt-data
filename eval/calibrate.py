"""Calibration map fitting. Stub: implemented in Phase 3.

Fits isotonic regression on a held-out BIRD split and emits eval/out/calibration.json,
which the server loads statically (it never fits anything at request time).
"""
